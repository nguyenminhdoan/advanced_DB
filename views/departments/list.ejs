<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | HR Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-users"></i> HR Management System
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="fas fa-home"></i> Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/employees"><i class="fas fa-user-tie"></i> Employees</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/departments"><i class="fas fa-building"></i> Departments</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/leaves"><i class="fas fa-calendar-alt"></i> Leave Requests</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/performance"><i class="fas fa-chart-line"></i> Performance</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="reportsDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-file-alt"></i> Reports
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/performance/top-performers">Top Performers</a></li>
                            <li><a class="dropdown-item" href="/reports/audit">Audit Log</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-4">
        <!-- Flash Messages -->
        <% if (typeof session !== 'undefined' && session.message) { %>
            <div class="alert alert-<%= session.message.type === 'error' ? 'danger' : session.message.type %> alert-dismissible fade show">
                <%= session.message.text %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% delete session.message; %>
        <% } %>

        <!-- Page Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h2"><i class="fas fa-building"></i> Departments</h1>
                <p class="text-muted">Department overview with average salary calculations</p>
            </div>
        </div>

        <!-- Departments Grid -->
        <div class="row">
            <% if (departments.length === 0) { %>
                <div class="col-12">
                    <div class="card">
                        <div class="card-body text-center py-5">
                            <i class="fas fa-building fa-3x text-muted mb-3"></i>
                            <h5>No Departments Found</h5>
                            <p class="text-muted">No departments are currently configured in the system.</p>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <% departments.forEach(dept => { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100">
                            <div class="card-header bg-primary text-white">
                                <h5 class="mb-0">
                                    <i class="fas fa-building"></i> <%= dept.DP_NAME %>
                                </h5>
                                <small>ID: <%= dept.DP_ID %></small>
                            </div>
                            <div class="card-body">
                                <dl class="row mb-3">
                                    <dt class="col-6">Manager:</dt>
                                    <dd class="col-6"><%= dept.MANAGER_NAME || 'Not assigned' %></dd>
                                    
                                    <dt class="col-6">Employees:</dt>
                                    <dd class="col-6">
                                        <span class="badge bg-info"><%= dept.EMPLOYEE_COUNT %></span>
                                    </dd>
                                    
                                    <dt class="col-6">Avg Salary:</dt>
                                    <dd class="col-6">
                                        <% if (dept.avgSalary > 0) { %>
                                            <strong class="text-success">$<%= dept.avgSalary.toLocaleString() %></strong>
                                            <br><small class="text-muted">Using PL/SQL function</small>
                                        <% } else { %>
                                            <span class="text-muted">No data</span>
                                        <% } %>
                                    </dd>
                                    
                                    <dt class="col-6">Created:</dt>
                                    <dd class="col-6"><%= new Date(dept.CREATED_DATE).toLocaleDateString() %></dd>
                                </dl>
                            </div>
                            <div class="card-footer bg-light">
                                <div class="d-flex justify-content-between">
                                    <a href="/departments/<%= dept.DP_ID %>/employees" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-users"></i> View Employees
                                    </a>
                                    <div class="btn-group btn-group-sm">
                                        <button class="btn btn-outline-secondary" title="Edit" disabled>
                                            <i class="fas fa-edit"></i>
                                        </button>
                                        <button class="btn btn-outline-info" title="Reports" disabled>
                                            <i class="fas fa-chart-bar"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>

        <!-- Department Statistics -->
        <div class="row mt-4">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="fas fa-chart-pie"></i> Department Size Distribution</h5>
                    </div>
                    <div class="card-body">
                        <% if (departments.length > 0) { %>
                            <% departments.forEach(dept => { %>
                                <div class="d-flex justify-content-between align-items-center mb-2">
                                    <span><%= dept.DP_NAME %></span>
                                    <div>
                                        <span class="badge bg-primary me-1"><%= dept.EMPLOYEE_COUNT %></span>
                                        <% if (dept.EMPLOYEE_COUNT > 0) { %>
                                            <small class="text-muted">employees</small>
                                        <% } %>
                                    </div>
                                </div>
                                <% if (dept.EMPLOYEE_COUNT > 0) { %>
                                    <div class="progress mb-3" style="height: 6px;">
                                        <div class="progress-bar" role="progressbar" 
                                             style="width: <%= (dept.EMPLOYEE_COUNT / Math.max(...departments.map(d => d.EMPLOYEE_COUNT))) * 100 %>%">
                                        </div>
                                    </div>
                                <% } %>
                            <% }); %>
                        <% } else { %>
                            <p class="text-muted mb-0">No department data available</p>
                        <% } %>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="fas fa-dollar-sign"></i> Salary Overview</h5>
                    </div>
                    <div class="card-body">
                        <% if (departments.length > 0) { %>
                            <% 
                            const salaryDepts = departments.filter(d => d.avgSalary > 0);
                            if (salaryDepts.length > 0) {
                                const maxSalary = Math.max(...salaryDepts.map(d => d.avgSalary));
                                const minSalary = Math.min(...salaryDepts.map(d => d.avgSalary));
                                const totalSalary = salaryDepts.reduce((sum, d) => sum + (d.avgSalary * d.EMPLOYEE_COUNT), 0);
                                const totalEmployees = salaryDepts.reduce((sum, d) => sum + d.EMPLOYEE_COUNT, 0);
                                const overallAvg = totalEmployees > 0 ? totalSalary / totalEmployees : 0;
                            %>
                                <div class="row text-center">
                                    <div class="col-4">
                                        <h6 class="text-success">$<%= maxSalary.toLocaleString() %></h6>
                                        <small class="text-muted">Highest Avg</small>
                                    </div>
                                    <div class="col-4">
                                        <h6 class="text-primary">$<%= overallAvg.toLocaleString() %></h6>
                                        <small class="text-muted">Overall Avg</small>
                                    </div>
                                    <div class="col-4">
                                        <h6 class="text-warning">$<%= minSalary.toLocaleString() %></h6>
                                        <small class="text-muted">Lowest Avg</small>
                                    </div>
                                </div>
                                
                                <hr>
                                
                                <% salaryDepts.forEach(dept => { %>
                                    <div class="d-flex justify-content-between mb-1">
                                        <small><%= dept.DP_NAME %></small>
                                        <small class="text-primary">$<%= dept.avgSalary.toLocaleString() %></small>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <p class="text-muted mb-0">No salary data available</p>
                            <% } %>
                        <% } else { %>
                            <p class="text-muted mb-0">No salary data available</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- PL/SQL Function Information -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="card bg-light">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="fas fa-database"></i> Database Integration</h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <h6><i class="fas fa-code text-primary"></i> PL/SQL Function Used</h6>
                                <code>get_dept_avg_salary(dept_id)</code>
                                <p class="small text-muted mt-2">
                                    This function calculates the average salary for each department by joining 
                                    employees, positions, and department tables. It includes exception handling 
                                    for departments that don't exist.
                                </p>
                            </div>
                            <div class="col-md-6">
                                <h6><i class="fas fa-cogs text-success"></i> Features</h6>
                                <ul class="small mb-0">
                                    <li>Real-time salary calculations</li>
                                    <li>Only includes active employees</li>
                                    <li>Exception handling for invalid departments</li>
                                    <li>Rounded to 2 decimal places</li>
                                    <li>Returns 0 for departments with no employees</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- Footer -->
    <footer class="bg-light mt-5 py-4">
        <div class="container text-center">
            <p class="mb-0 text-muted">&copy; 2024 HR Management System - COMP214 Database Project</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/script.js"></script>
</body>
</html>